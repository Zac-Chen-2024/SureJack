/**
 * 前后端必须一致的常量。
 *
 * 【为什么不直接 import 后端的】：后端那份在 src/db/user-db.ts 里，
 * 那个模块依赖 better-sqlite3（原生模块），import 它会把整个数据库驱动
 * 拖进浏览器包，构建直接失败。
 *
 * 所以只能各写一份。改任何一个值时【两处都要改】：
 *   后端 src/db/user-db.ts
 *   前端 web/src/constants.ts（本文件）
 */

/**
 * 背景音乐的默认音量（相对配音）。
 *
 * 15%：配音必须始终压过 BGM，营销号的信息全在人声里。
 * 10% 试下来偏轻，20% 开始抢话。
 *
 * 只作用于【新建】项目——已有项目存的是自己的值，不会被这个数覆盖。
 */
export const DEFAULT_BGM_VOLUME = 0.15
